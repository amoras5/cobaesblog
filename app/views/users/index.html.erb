<% provide(:title, 'Usuarios Registrados') %>
<h1 style="text-align: right;">Usuarios Registrados (<%= User.count %>)</h1>
<%= form_tag users_path, :name => "searchbar", :method => 'get' do %>
  <div class="input-append">
  	<%= text_field_tag :search, params[:search], class: 'span2', id: "appendedInputButtons"  %>
    <%= submit_tag "Buscar", :name => nil, class: 'btn' %>
    <%= button_tag "Quitar búsqeda", :name => nil, :onclick => 'clear_search_bar();', class: 'btn' %>
  </div>
<% end %>
<%= will_paginate @users, :previous_label => 'Anterior', :next_label => 'Siguiente' %>
<ul class="users">
  <%= render @users %>
</ul>
<%= will_paginate @users, :previous_label => 'Anterior', :next_label => 'Siguiente' %>