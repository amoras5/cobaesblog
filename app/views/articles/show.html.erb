<% provide(:title, @article.title) %>
<h2 style="text-align: left;"><%= @article.title %></h2>
<div class="marco">
  <%= @article.content.html_safe %>
</div>
<% if current_user.id == @article.user_id || current_user.admin? %>
  <%= link_to "Editar Artículo", edit_article_path(@article.id) %></a>
  <%= link_to "| Borrar Artículo", article_path, method: :delete, data: { confirm: "Estás seguro?" } %>
<% end %>
<br /><hr>
<% if signed_in? %>
   <div class="row">
      <aside class="span4">
         <section>
         	<%= render 'shared/micropost_form' %>
         </section>
      </aside>
      <div class="span8" id="articlesfeed">
         <%= render 'shared/feed' %>
      </div>
   </div>
<% end %>