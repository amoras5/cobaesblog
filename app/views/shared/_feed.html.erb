<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render partial: 'shared/feed_item' %>
  </ol>
  <%= will_paginate @feed_items, :previous_label => 'Anterior', :next_label => 'Siguiente' %>
<% end %>