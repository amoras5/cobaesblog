<%= form_for(@article) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
  	<%= f.text_area :content, placeholder: "Escribe aquí tu artículo..." %>
  </div>
  <%= f.submit "Publicar", class: "btn btn-large btn-success" %>
<% end %>